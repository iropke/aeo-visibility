# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
# Pydantic EmailStr 의존 — F3-pricing 청크 (Contact 폼 검증).
email-validator==2.2.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Redis (caching)
redis==5.2.1

# HTTP / Crawling
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0

# Scoring utilities
python-whois==0.9.5
textstat==0.7.4
# textstat 0.7.4 가 pkg_resources 를 임포트하는데 setuptools 81+ 에서 분리됨.
# Phase 1 안정성 우선 — pkg_resources 가 동작하도록 setuptools<81 로 핀.
# 후속: textstat 0.7.5+ 업그레이드 시점에 setuptools 핀 해제 검토.
setuptools<81

# AI APIs
anthropic==0.43.0

# Email
resend==2.5.1

# Templates
jinja2==3.1.5

# CORS
python-multipart==0.0.20

# v2 Auth (Supabase JWT)
python-jose[cryptography]==3.3.0
